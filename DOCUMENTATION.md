# مستند پروژه Topic Classifier

طبقه‌بندی چندبرچسبی موضوع برای پست‌های فارسی ویرگول.

---

## هدف پروژه

ساخت سامانه‌ای که در لحظهٔ انتشار، به هر پست وبلاگ فارسی در پلتفرم **ویرگول** یک یا چند موضوع از میان حدود **۵۰ موضوع ازپیش‌تعریف‌شده** نسبت دهد.

خروجی نهایی باید برای بک‌اند قابل استفاده باشد: لیستی از نام موضوع‌ها (یا خالی بودن لیست وقتی هیچ موضوعی مناسب نیست). سیستم باید **کاملاً آفلاین و محلی** اجرا شود (بدون API ابری)، روی سخت‌افزار محدود (RTX 3050) و متن فارسی را — از جمله کنایه، اصطلاح و ارجاع فرهنگی — درست بفهمد.

---

## مسئله‌ای که حل می‌کنی

برچسب‌گذاری دستی/شرکتی پست‌ها **نویزی و غیرقابل اعتماد** است. پست‌ها چندموضوعی‌اند، زبان محاوره و ارجاعات فرهنگی دارند (مثلاً «شاگردان کی‌روش» به‌معنای تیم ملی فوتبال ایران)، و بخشی از محتوا اصلاً داخل تاکسونومی ۵۰تایی جا نمی‌گیرد (تبلیغ محصول، خاطرهٔ روزانه، پست متا دربارهٔ خود ویرگول).

مسئلهٔ عملی این است:

1. از روی متن پست (عنوان + بدنه) چند موضوع کاندید پیدا شود، بدون اینکه نویز برچسب‌های موجود را حفظ کنیم.
2. یک مدل زبانی محلی تصمیم نهایی چندبرچسبی را بگیرد: کدام کاندید(ها) واقعاً موضوع پست‌اند، یا اینکه هیچ‌کدام مناسب نیست.
3. پست‌هایی که در تاکسونومی جا نمی‌گیرند برای بازبینی انسانی صف شوند، نه اینکه به‌زور به یکی از ۵۰ موضوع چسبانده شوند.

معیار ارزیابی هدف، دقت خام (accuracy) نیست؛ چون مسئله **چندبرچسبی** است، باید precision / recall / F1 در سطح موضوع و کلی گزارش شود. مجموعهٔ تست ایده‌آل باید جدا از دادهٔ ساخت centroid باشد و دستی تأیید شود.

---

## معماری پروژه

معماری انتخاب‌شده **retrieve → verify** در دو مرحله است:

```
پست جدید (عنوان + بدنه)
        │
        ▼
  پیش‌پردازش متن فارسی
        │
        ▼
  Embedding با BGE-M3 (Ollama)
        │
        ▼
  مقایسهٔ کسینوسی با ۵۰ centroid موضوع
        │
        ▼
  Top-5 کاندید + برچسب strong/weak
        │
        ▼
  LLM محلی (qwen3:4b) روی متن کامل + نام کاندیدها
        │
        ├── match     → لیست موضوع‌ها برای بک‌اند
        └── no_match  → topics=[] + صف بازبینی انسانی
```

### مرحله ۱ — بازیابی (retrieve)

- برای هر موضوع، یک بردار **centroid** ساخته می‌شود: میانگین embedding پست‌هایی که آن موضوع را در برچسب دارند.
- embedding پست جدید با همهٔ centroidها با **cosine similarity** مقایسه می‌شود.
- پنج موضوع نزدیک برمی‌گردند. امتیاز بالای آستانهٔ `0.6134` به‌عنوان `strong` و پایین‌تر به‌عنوان `weak` علامت می‌خورد. آستانه فعلاً فقط برچسب اطمینان است؛ کاندیدها حذف نمی‌شوند تا LLM همیشه پنج گزینه داشته باشد.

### مرحله ۲ — تأیید (verify)

- متن تمیزشدهٔ پست + فهرست پنج کاندید (نام موضوع، امتیاز، strong/weak) به LLM داده می‌شود.
- مدل باید JSON معتبر بدهد:
  - `decision: "match"` + `topics` (زیرمجموعه‌ای از همان پنج نام) + `reason`
  - یا `decision: "no_match"` + `suggested_topic` + `reason`
- در حالت `no_match` موضوعی به بک‌اند نوشته نمی‌شود و رکورد در صف CSV می‌رود.

شاخهٔ جانبی (برای بعد): از فاصلهٔ centroidها می‌توان موضوع‌های قابل ادغام (centroid خیلی شبیه) یا قابل شکستن (پست‌های دور از centroid خودشان) را پیدا کرد. این شاخه هنوز محصول نیست.

---

## ساختار فایل‌ها

```
topic-classifier/
├── DOCUMENTATION.md          # همین مستند
├── HANDOFF.md                # زمینهٔ ادامهٔ کار برای عامل/توسعه‌دهنده
├── requirements.txt          # وابستگی‌های پایتون
├── src/                      # هستهٔ pipeline تولید
│   ├── config.py             # مسیرهای مشترک (CSV، centroid)
│   ├── preprocess.py         # پاک‌سازی متن فارسی
│   ├── post_text.py          # ساخت متن قابل‌embed از عنوان+بدنه
│   ├── embeddings.py         # فراخوانی مدل embedding از Ollama
│   ├── centroids.py          # ساخت/ذخیره/بارگذاری centroidها
│   └── retrieve.py           # بازیابی top-k موضوع
├── scripts/                  # اسکریپت‌های ارزیابی و مرحلهٔ ۲ (نه API محصول)
│   ├── llm_verify.py         # تأیید LLM روی مجموعهٔ تست
│   ├── llm_verify_compare.py # مقایسهٔ qwen3:4b و gemma3:4b
│   ├── smoke_retrieve_posts.py
│   ├── validate_retrieval.py
│   ├── validate_retrieval_examples.py
│   ├── threshold_analysis.py
│   └── inspect_misses.py
└── data/
    ├── posts.csv             # دیتاست پست‌ها
    ├── centroids.npz         # خروجی ساخت centroid (باید ساخته شود)
    ├── llm_verify_run.log
    ├── llm_verify_compare.log
    └── llm_no_match_queue.csv  # صف no_match (پس از اجرا ساخته می‌شود)
```

پوشهٔ اکتشافی `topic_discovery/` حذف شده است. کش embedding (`cache/embeddings.npy` و `cache/post_ids.npy`) در مستندات قبلی ذکر شده؛ در `src/` لودر اختصاصی برای آن وجود ندارد.

---

## توضیح هر بخش

### `src/config.py`

مسیر ریشهٔ پروژه، `data/posts.csv` و `data/centroids.npz` را متمرکز نگه می‌دارد تا اسکریپت‌ها مسیر را تکرار نکنند.

### `src/preprocess.py` — `clean_text`

ترتیب کار:

1. نرمال‌سازی یونیکد NFKC
2. نگاشت عربی → فارسی (`ي→ی`، `ك→ک`، `ة→ه`، …) و حذف کشیده (ـ)
3. حذف علائم جهت/کنترل نامرئی؛ **حفظ نیم‌فاصله (ZWNJ)**
4. حذف Markdown و تگ HTML
5. جمع‌کردن فاصله‌های سفید

عمداً stopword، stemming، حذف علائم و ایموجی انجام نمی‌شود؛ برای embedding این کار کیفیت را پایین می‌آورد.

### `src/post_text.py`

عنوان و بدنه را جداگانه تمیز می‌کند، به هم می‌چسباند، و تا **۶۰۰۰ کاراکتر** کوتاه می‌کند. بعضی بدنه‌ها بیش از ۱۰۰ هزار کاراکتر دارند و از پنجرهٔ زمینهٔ BGE-M3 رد می‌شوند.

### `src/embeddings.py`

کلاینت Ollama روی `http://localhost:11434` با مدل `bge-m3`. توابع `get_embedding` و `get_embeddings`.

### `src/centroids.py`

- CSV را با موتور python و رد خطوط خراب می‌خواند (بدنه‌های HTML بزرگ).
- روی `post_id` یکتا می‌کند.
- موضوعات را از رشتهٔ ویرگول‌جدا پارس می‌کند.
- هر پست را embed می‌کند، با checkpoint میانی تا قطع شدن اجرا کار را از دست ندهد.
- برای هر موضوع میانگین بردارها را می‌گیرد و در NPZ ذخیره می‌کند (`topics` + `centroids`).

### `src/retrieve.py`

`retrieve_candidates(title, body, centroids, top_k=5, threshold=0.6134)` لیست دیکشنری با `topic`، `similarity` و `confidence` برمی‌گرداند. نقطهٔ ورود اصلی مرحلهٔ ۱ برای مرحلهٔ ۲ و اسکریپت‌های تشخیصی است.

### اسکریپت‌ها

| اسکریپت | نقش |
|--------|-----|
| `llm_verify.py` | مرحلهٔ ۲ روی مجموعهٔ تست؛ چاپ payload بک‌اند؛ صف `no_match` |
| `llm_verify_compare.py` | همان پرومپت روی qwen و gemma؛ بدون نوشتن بک‌اند/CSV |
| `validate_retrieval.py` | hit-rate: آیا موضوع شرکتی در top-5 هست؟ |
| `validate_retrieval_examples.py` | hit@1 / hit@5 برای موضوعات نمونه (ورزش، غذا، تکنولوژی) |
| `threshold_analysis.py` | توزیع شباهت موضوع خودی در برابر موضوع تصادفی و پیشنهاد آستانه |
| `inspect_misses.py` | چاپ بدنهٔ تمیز پست‌هایی که موضوع هدف در top-5 نیست |
| `smoke_retrieve_posts.py` | تست دستی روی چند پست هاردکد فارسی |

---

## مدل‌هایی که استفاده کرده‌ای

| نقش | مدل | محل اجرا | وضعیت |
|-----|------|----------|--------|
| Embedding | **bge-m3** | Ollama، محلی | مدل تولید مرحلهٔ ۱ |
| Verify LLM | **qwen3:4b** | Ollama، محلی | مدل فعلی مرحلهٔ ۲ |
| مقایسهٔ Verify | **gemma3:4b** | Ollama، محلی | فقط آزمایش کناره‌به‌کناره |
| Embedding آزمایش‌شده | qwen3-embedding:4b | Ollama | رد شد |
| Embedding در نظر گرفته‌شده | Hakim (فارسی) | API آنلاین | رد شد: نیاز به اینترنت/سرور خارج از دسترس از ایران |
| طبقه‌بند مستقیم | ParsBERT fine-tune | — | رد شد |

وابستگی‌های پایتون: `numpy`، `pandas`، `scikit-learn`، `ollama`.

---

## نتایج فعلی

اعداد زیر از اجرای واقعی روی همین ریپو و از ممیزی دستی قبلی است. برچسب‌های شرکتی ground truth تمیز نیستند؛ بنابراین hit-rate نسبت به آن‌ها حد پایین کیفیت بازیابی است، نه سقف آن.

### داده

- حدود **۷۴۶۳** پست یکتا پس از حذف تکرار `post_id`
- **۵۰** centroid موضوع در `data/centroids.npz` (پس از اجرای `centroids.py`)
- CSV: `post_id, title, body, sampled_topic, topics, tags` — موضوعات چندتایی با ویرگول جدا شده‌اند

### ممیزی centroid در برابر برچسب شرکت (۴۰ پست نمونه‌گیری‌شده)

مقایسهٔ سه‌طرفه: برچسب شرکت در برابر top-5 centroid در برابر زمینهٔ خوشهٔ HDBSCAN.

- پیش‌بینی centroid در حدود **۹۷٪** موارد برابر یا بهتر از برچسب شرکت بود.
- **۱۵ از ۴۰**: انتساب centroid مشخصاً دقیق‌تر از برچسب شرکت.
- **۰ از ۴۰**: برچسب شرکت مشخصاً بهتر از centroid.

نتیجه: بازیابی centroid برای ورود به مرحلهٔ ۲ معتبر است؛ نویز اصلی در برچسب‌های موجود است.

### ممیزی تاکسونومی با HDBSCAN

`min_cluster_size=30`، **۲۷ خوشه** روی ۷۴۶۳ پست:

- حدود **۱۹ خوشه** یک موضوع شرکتی غالب دارند (بیش از ۵۰٪ پست‌های خوشه) — تاکسونومی آنجا پایدار است.
- چند خوشه موضوع‌های مرتبط را مخلوط می‌کنند (مثلاً تاب‌آوری، محتواهای فلسفی/معنوی، فرزندپروری) — اصلاح تاکسونومی، نه شکست بازیابی.
- خوشهٔ ۱ (~۲۰٪، ۱۴۹۷ پست): سبک تبلیغ/توصیف محصول؛ با ۵۰ موضوع جور نیست — تصمیم محصولی باز.
- خوشهٔ ۲۶ (~۹٫۵٪): روایت/خاطرهٔ اول‌شخص — همان سؤال باز.
- خوشهٔ ۲۵ (~۰٫۴٪): پست متا دربارهٔ ویرگول — همان سؤال باز.
- نویز (~۳۱٫۶٪، برچسب −۱): کامل توصیف نشده.

### مرحلهٔ ۲ — LLM verify (`qwen3:4b`، ۱۹ پست تست)

از `data/llm_verify_run.log`:

- `match`: **۱۷**
- `no_match`: **۰**
- `error`: **۲** (پاسخ JSON خارج از اسکیما؛ مثلاً کلید `"match": true` به‌جای `"decision": "match"`)
- `missing`: **۰**

نکته: مدل تقریباً همیشه match می‌دهد و هنوز روی پست‌های تبلیغ/خاطره/متا به‌اندازهٔ کافی `no_match` نمی‌زند. پرامپت بعداً سخت‌گیرانه‌تر شده (فیلد اجباری `reason` و مثال no_match تبلیغاتی) ولی مجموعهٔ ۱۹تایی کامل دوباره با همین پرامپت روی دیسک ثبت نشده است.

در کد فعلی `TEST_POST_IDS = TEST_POST_IDS[:3]` است؛ یعنی اجرای پیش‌فرض اسکریپت فقط **۳ پست اول** را می‌زند، نه ۱۹ پست کامل.

### مقایسهٔ qwen3:4b و gemma3:4b (۱۹ پست)

از `data/llm_verify_compare.log`:

- هر دو مدل پاسخ قابل‌پارس: **۱۷**
- توافق کامل روی تصمیم+موضوع‌ها: **۵**
- اختلاف / توافق ناقص: **۱۲**
- خطای مدل: **۲**

qwen معمولاً موضوعات کمتری انتخاب می‌کند؛ gemma تمایل به لیست طولانی‌تر دارد. انتخاب فعلی مرحلهٔ ۲ همچنان **qwen3:4b** است (سخت‌گیری بیشتر در انتخاب موضوع)، نه gemma.

---

## تصمیم‌هایی که گرفته‌ای و دلیلشان

1. **retrieve → verify به‌جای یک طبقه‌بند واحد**  
   برچسب‌ها نویزی‌اند. مرحلهٔ بازیابی فضای جستجو را به چند نام محدود می‌کند؛ LLM روی متن واقعی تصمیم می‌گیرد، نه روی برچسب قدیمی.

2. **Centroid به‌جای KNN**  
   میانگین بردارهای یک موضوع نویز برچسب را نرم می‌کند. یک پست غلط‌برچسب‌خورده کل موضوع را منحرف نمی‌کند، برخلاف رأی نزدیک‌ترین همسایه‌ها.

3. **BGE-M3 به‌جای qwen3-embedding:4b**  
   روی جفت‌های فارسی با ارجاع فرهنگی، شباهت BGE-M3 حدود **۰٫۷۸** در برابر **۰٫۴۵** برای qwen بود. روی پارافریز تمیز و کنایه هم برابر یا بهتر بود.

4. **Hakim استفاده نشد**  
   وزن محلی از مسیر sentence-transformers ممکن است؛ API رسمی به سروری نیاز دارد که از ایران در دسترس نبود. محدودیت پروژه اجرای کاملاً آفلاین است.

5. **Fine-tune مستقیم ParsBERT رد شد**  
   مدل نویز برچسب را حفظ می‌کند. ParsBERT برای MLM آموزش دیده، نه برای شباهت contrastive؛ برای retrieve مناسب نیست.

6. **BM25 فعلاً لازم نیست**  
   تطبیق کلمهٔ دقیق از طریق دادن متن خام و تگ‌ها به LLM پوشش داده می‌شود.

7. **پیش‌پردازش سبک**  
   حذف stopword/stemming برای embedding ضرر دارد. نیم‌فاصله باید بماند چون در فارسی معنی‌دار است.

8. **آستانهٔ ۰٫۶۱۳۴ فقط برای برچسب اطمینان**  
   نقطهٔ میانی میانهٔ شباهت «موضوع خودی» و «موضوع تصادفی». اگر کاندیدهای زیر آستانه حذف شوند، درصد قابل توجهی از پست‌ها صفر کاندید می‌گیرند؛ بنابراین top-5 همیشه به LLM می‌رود.

9. **خروجی LLM محدود به JSON با `decision`**  
   تا بتوان match را به بک‌اند نوشت و no_match را صف کرد، بدون متن آزاد غیرقابل‌پارس.

10. **صف انسانی برای no_match**  
    تاکسونومی ۵۰تایی کامل نیست. بهتر است موضوع اشتباه نچسبانیم تا اینکه موضوع ساختگی به اجبار انتخاب شود.

11. **کد اکتشاف HDBSCAN از ریپو حذف شد**  
    ممیزی تاکسونومی بسته شد؛ pipeline تولید همان `src/` است، نه اسکریپت‌های خوشه‌بندی.

---

## کارهای باقی‌مانده

- برگرداندن مجموعهٔ تست کامل (۱۹ پست، نه برش `[:3]`) و اجرای دوبارهٔ verify با پرامپت فعلی.
- ساخت مجموعهٔ تست **دستی‌تأییدشده** حدود ۱۰۰–۱۵۰ پست، جدا از دادهٔ ساخت centroid؛ سپس precision / recall / F1 چندبرچسبی.
- تصمیم محصولی برای خوشه‌های خارج از تاکسونومی: تگ جدید (مثلاً تبلیغات / خاطره / متا)، حذف از طبقه‌بندی، یا هر دو.
- کاهش خطای پارس JSON (۲ از ۱۹) — مثلاً retry یا نرمال‌سازی کلیدهای نزدیک به اسکیما.
- بررسی اینکه qwen هنوز بیش‌ازحد match می‌دهد؛ در صورت نیاز پرامپت یا مدل verify عوض شود.
- اتصال واقعی به بک‌اند (الان فقط `BACKEND WRITE` چاپ می‌شود).
- استفادهٔ عملی از تگ‌های پست در پرامپت verify (در داده هست؛ در پرامپت فعلی کاندیدها از centroid می‌آیند و تگ خام جداگانه تزریق نمی‌شود).
- شاخهٔ ادغام/شکستن موضوع از روی centroidها.
- در صورت نیاز، وزن محلی Hakim از مسیر sentence-transformers.
- لودر اختیاری برای کش `cache/embeddings.npy` تا embedding تکراری نشود.

---

## نحوهٔ اجرای پروژه در سیستم خودت

پیش‌نیازها: Python 3، [Ollama](https://ollama.com) در حال اجرا، GPU محلی (در این پروژه RTX 3050). همهٔ مدل‌ها باید از قبل pull شده باشند؛ اینترنت برای استنتاج لازم نیست.

### ۱. مدل‌ها را در Ollama بیاور

```bash
ollama pull bge-m3
ollama pull qwen3:4b
# فقط اگر مقایسه می‌خواهی:
ollama pull gemma3:4b
```

مطمئن شو سرویس روی `http://localhost:11434` گوش می‌دهد.

### ۲. محیط پایتون

در ریشهٔ پروژه:

```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

روی لینوکس/مک به‌جای Activate از `source venv/bin/activate` استفاده کن.

### ۳. داده

فایل `data/posts.csv` باید موجود باشد (UTF-8). اگر `data/centroids.npz` نداری، یک‌بار centroid بساز:

```powershell
python -m src.centroids
```

این کار همهٔ پست‌های یکتا را از Ollama embed می‌کند و ممکن است طولانی باشد. اگر وسط کار قطع شود، از checkpoint ادامه می‌دهد.

### ۴. مرحلهٔ ۱ — بازیابی

```powershell
python -m src.retrieve
```

دو مثال هاردکد و اولین ردیف CSV را چاپ می‌کند.

تست دود روی چند پست دستی:

```powershell
python scripts/smoke_retrieve_posts.py
```

تشخیص کیفیت بازیابی:

```powershell
python scripts/validate_retrieval.py
python scripts/validate_retrieval_examples.py
python scripts/threshold_analysis.py
python scripts/inspect_misses.py
```

### ۵. مرحلهٔ ۲ — تأیید LLM

```powershell
python scripts/llm_verify.py
```

خروجی شامل preview مرحلهٔ ۱، JSON خام مدل، `reason`، و خط `BACKEND WRITE` است. موارد `no_match` به `data/llm_no_match_queue.csv` اضافه می‌شوند.

مقایسهٔ دو مدل (بدون نوشتن صف/بک‌اند):

```powershell
python scripts/llm_verify_compare.py
```

اگر خروجی فارسی در کنسول ویندوز خراب است، اسکریپت‌ها سعی می‌کنند encoding را عوض کنند؛ در صورت نیاز از UTF-8 در ترمینال استفاده کن (`chcp 65001`).

### ۶. محدودیت فعلی اجرا

در `scripts/llm_verify.py` لیست تست با `TEST_POST_IDS = TEST_POST_IDS[:3]` به سه پست محدود شده. برای اجرای کامل، این خط را بردار.
